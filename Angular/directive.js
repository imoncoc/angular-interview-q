// 1. What is directive?
// TODO: Directives allow you to manipulate the DOM, apply logic, and control how elements behave or appear.

    // Component: Contains a template and is used to define UI components.(<app-my-component>)
    // Attribute Directive: Alters the behavior, appearance, or style of an existing element or component.(ngClass, ngStyle)
    // Structural Directive:  Modifies the structure of the DOM (*ngIf, *ngFor)