
// 2. Http Client, call API?
// the HttpClient is a built-in service used to make HTTP requests to a server or API. It is part of Angular's @angular/common/http package and allows you to easily fetch, send, or update data between your application and a backend API.

// 3. Promise All
// TODO: The Promise.all() static method takes an iterable of promises as input and returns a single Promise. This returned promise fulfills when all of the input's promises fulfill (including when an empty iterable is passed), with an array of the fulfillment values. It rejects when any of the input's promises rejects, with this first rejection reason.

// 4. how to communicate component to component in angular
// TODO: 
// i) Input -> parent to child 
// i) Output -> child to parent
// i) Service -> sibling communicate -> subject, behavior Subject
// i) @ViewChild -> Used to access a single child component, directive, or DOM
// i) @ViewChildren -> Used to access multiple child components, directives, or DOM
// i) @ContentChild -> Used to access a single projected child component, directive, or DOM
// i) @ContentChildren -> Used to access multiple projected child components, directives, or DOM

// 5. Modular pattern in angular?
// TODO: A module in angular is a way of bundling the angular building blocks like components, directives, services, pipe etc together. A module get's scanned by Angular to be aware of what features are begin used in that Angular project.

// 6. How service class works?
// TODO: A service in Angular is a re-usable TypeScript class that can be used in multiple components across our Angular application

